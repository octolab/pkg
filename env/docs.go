// Package env contains abstractions to work with
// environment variables, such as Variable and Environment.
// It also includes functions for better integration
// with the built-in os package.
package env
